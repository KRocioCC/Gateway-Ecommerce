# API Gateway

## Configuración de Keycloak

El API Gateway utiliza Keycloak para la autenticación mediante OAuth2 y JWT.

El realm configurado es:

```text
ecommmerce-realm
```
http://localhost:8080/realms/ecommmerce-realm/.well-known/openid-configuration